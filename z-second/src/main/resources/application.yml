server:
  port: 9094
spring:
  application:
    name: z-second
  output:
    ansi:
      enabled: always
management:
  endpoints:
    metrics:
      enabled: true
    info:
      enabled: true
    web:
      exposure:
        include: "*"